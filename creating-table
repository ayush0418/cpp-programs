#include<iostream>
using namespace std;

int main()
{
       
       int n;

       cout<<"enter a positive integer: "
       cin>>n;

       for(int i=1;i<=n; i++)
       {
              cout<< n << " * "<< i <<" = "<< n*i;
       }

       return 0;
}
